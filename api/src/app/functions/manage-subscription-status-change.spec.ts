import { beforeEach, describe, it, vi } from "vitest";

describe("manage subscription status change", () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it("should create new customer and subscription", async () => {});

  it("should update existing subscription", async () => {});
});
